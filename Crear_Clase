'use strict'
class Usuario {
    constructor (Apellido, Nombre, edad){
        this.Apellido = Apellido;
        this.Nombre = Nombre;
        this.edad = edad;
    }
}
const nuevoUsuario = () => {
    return new Usuario("Angela", "Arteaga", 38);
};

console.log(nuevoUsuario());
