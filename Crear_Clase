'use strict'
class User {
    constructor (Apellido, Nombre, edad){
        this.Apellido = Apellido;
        this.Nombre = Nombre;
        this.edad = edad;
    }
}
const getUser = () => {
    return new User("Angela", "Arteaga", 38);
};

//sample usage
console.log(getUser());
